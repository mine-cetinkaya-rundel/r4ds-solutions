---
title: "Workflow: basics"
---

## Solutions

```{r}
#| label: load-packages
#| message: false

library(tidyverse)
```

## 3.5 Exercises

1.  Because in `my_variable` theres a 1 (i without dot) instead of an i in variable.

2.  

```{r}
library(tidyverse)
ggplot(mpg) + 
  geom_point(aes(x = displ, y = hwy)) +
  geom_smooth(method = "lm")
```

3.  Alt + Shift + K pulls up keyboard shortcuts.

4. The `my_bar_plot` object is saved because that's what was specified in the `plot` argument of the `ggsave()` call.

```{r}
my_bar_plot <- ggplot(mpg, aes(x = class)) +
  geom_bar()
my_scatter_plot <- ggplot(mpg, aes(x = cty, y = hwy)) +
  geom_point()
ggsave(filename = "mpg-plot.png", plot = my_bar_plot)
```
